<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <title>{{companyName}} - e-katalog</title>
  <style>
    @font-face {
      font-family: 'Roboto';
      src: url('data:application/font-woff;charset=utf-8;base64,{{robotoRegularFont}}') format('woff');
      font-weight: normal;
      font-style: normal;
    }
    
    @font-face {
      font-family: 'Roboto';
      src: url('data:application/font-woff;charset=utf-8;base64,{{robotoBoldFont}}') format('woff');
      font-weight: bold;
      font-style: normal;
    }
    
    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      font-family: 'Roboto', Arial, sans-serif;
    }
    
    /* Kapak Sayfası Stilleri */
    .cover-page {
      width: 100%;
      height: 100vh;
      background: linear-gradient(135deg, #8B4513 0%, #D2691E  50%, #F4A460 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      position: relative;
      page-break-after: always;
      background-image: url('{{backgroundImage}}');
      background-size: cover;
      background-position: center;
    }
    
    .cover-page::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.3);
      z-index: 1;
    }
    
    .cover-content {
      position: relative;
      z-index: 2;
      text-align: center;
      color: white;
      padding: 40px;
    }
    
    .logo-container {
      background: rgba(255, 255, 255, 0.95);
      padding: 30px;
      border-radius: 15px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    }
    
    .company-logo {
      width: 200px;
      height: auto;
      margin-bottom: 20px;
    }
    
    .company-name {
      font-size: 48px;
      font-weight: bold;
      color: #8B4513;
      margin: 0;
      text-shadow: none;
    }
    
    .catalog-title {
      font-size: 32px;
      color: #D2691E;
      margin: 10px 0;
      font-weight: normal;
    }
    
    .website {
      font-size: 18px;
      color: rgba(255, 255, 255, 0.9);
      margin-top: 20px;
    }
    
    /* İç Sayfa Stilleri */
    .content-page {
      width: 100%;
      min-height: 100vh;
      background: white;
      padding: 40px;
      box-sizing: border-box;
      page-break-before: always;
    }
    
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
      padding-bottom: 20px;
      border-bottom: 3px solid #D2691E;
    }
    
    .page-logo {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    
    .page-logo-img {
      width: 60px;
      height: 60px;
      background: #8B4513;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 24px;
    }
    
    .page-company-name {
      font-size: 24px;
      font-weight: bold;
      color: #8B4513;
    }
    
    .page-catalog-info {
      text-align: right;
      color: #666;
    }
    
    .collection-title {
      font-size: 36px;
      font-weight: bold;
      color: #D2691E;
      text-align: center;
      margin: 30px 0 50px 0;
      letter-spacing: 1px;
    }
    
    /* Ürün Grid Sistemi */
    .products-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 40px;
      margin-bottom: 60px;
    }
    
    .product-item {
      background: white;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.2s ease;
    }
    
    .product-item:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
    }
    
    .product-image-container {
      width: 100%;
      height: 250px;
      background: #f8f8f8;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }
    
    .product-image {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
    }
    
    .no-image {
      color: #999;
      font-size: 14px;
      text-align: center;
    }
    
    .product-info {
      padding: 20px;
      text-align: center;
    }
    
    .product-code {
      font-size: 12px;
      color: #999;
      text-transform: uppercase;
      margin-bottom: 8px;
      letter-spacing: 0.5px;
    }
    
    .product-name {
      font-size: 16px;
      font-weight: bold;
      color: #333;
      margin: 0;
      line-height: 1.3;
    }
    
    .product-description {
      font-size: 12px;
      color: #666;
      margin-top: 8px;
      line-height: 1.4;
    }
    
    /* Sayfa Numarası */
    .page-number {
      position: fixed;
      bottom: 30px;
      right: 50%;
      transform: translateX(50%);
      background: #D2691E;
      color: white;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: bold;
    }
    
    /* Print Stilleri */
    @media print {
      .cover-page, .content-page {
        page-break-inside: avoid;
      }
      
      .product-item:hover {
        transform: none;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
    }
  </style>
</head>
<body>
  <!-- Kapak Sayfası -->
  <div class="cover-page">
    <div class="cover-content">
      <div class="logo-container">
        <div class="company-logo">
          <!-- Logo buraya gelecek -->
          <div style="width: 120px; height: 120px; background: #8B4513; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px;">
            <span style="color: white; font-size: 36px; font-weight: bold;">PH</span>
          </div>
        </div>
        <h1 class="company-name">{{companyName}}</h1>
        <p class="catalog-title">e-katalog</p>
      </div>
      
      <p class="website">www.pashahome.com</p>
    </div>
  </div>

  <!-- İçerik Sayfaları -->
  {{#each collections}}
    <div class="content-page">
      <!-- Sayfa Başlığı -->
      <div class="page-header">
        <div class="page-logo">
          <div class="page-logo-img">PH</div>
          <span class="page-company-name">{{../companyName}}</span>
        </div>
        <div class="page-catalog-info">
          <div>e-katalog</div>
        </div>
      </div>
      
      <!-- Koleksiyon Başlığı -->
      <h2 class="collection-title">{{collectionName}}</h2>
      
      <!-- Ürünler Grid -->
      <div class="products-grid">
        {{#each products}}
          <div class="product-item">
            <div class="product-image-container">
              {{#if productImage}}
                <img src="{{productImage}}" alt="{{name}}" class="product-image">
              {{else}}
                <div class="no-image">Görsel Mevcut Değil</div>
              {{/if}}
            </div>
            <div class="product-info">
              <h3 class="product-name">{{name}}</h3>
              {{#if description}}
                <p class="product-description">{{description}}</p>
              {{/if}}
            </div>
          </div>
        {{/each}}
      </div>
      
      <!-- Sayfa Numarası -->
      <div class="page-number">{{pageNumber}}</div>
    </div>
  {{/each}}
</body>
</html> 