{
  "routes": [
    {
      "method": "POST",
      "path": "/auth/custom-login",
      "handler": "custom-login.login",
      "config": {
        "policies": [],
        "description": "Custom login endpoint",
        "tag": {
          "plugin": "api",
          "name": "Auth"
        }
      }
    },
    {
      "method": "GET",
      "path": "/auth/me",
      "handler": "custom-login.getMe",
      "config": {
        "policies": ["global::is-authenticated"],
        "description": "Get current user information",
        "tag": {
          "plugin": "api",
          "name": "Auth"
        }
      }
    }
  ]
} 